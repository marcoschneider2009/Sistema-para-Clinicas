<%- include('partials/header') %>
<%- include('partials/sidebar') %>

<main class="content-wrapper">
    <div class="page-header" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px;">
        <div>
            <h1><i class="fas fa-user-md"></i> Gestão de Profissionais</h1>
            <p>Lista de especialistas cadastrados no sistema.</p>
        </div>
        
        <div style="flex: 1; margin: 0 30px;">
            <div style="position: relative;">
                <i class="fas fa-search" style="position: absolute; left: 12px; top: 12px; color: #94a3b8;"></i>
                <input type="text" id="inputPesquisa" onkeyup="filtrarProfissionais()" placeholder="Pesquisar profissional pelo nome..." 
                       style="width: 100%; padding: 10px 10px 10px 40px; border: 1px solid #e2e8f0; border-radius: 8px;">
            </div>
        </div>

        <button class="btn-salvar" onclick="abrirModalCadastro()" style="background: #1e293b; color: white; padding: 12px 24px; border-radius: 6px; cursor: pointer; border: none;">
            <i class="fas fa-plus"></i> Novo Profissional
        </button>
    </div>

    <table class="aura-table" id="tabelaProfissionais">
        <thead>
            <tr>
                <th style="width: 50px;">Foto</th> 
                <th>Nome do Profissional</th>
                <th>Especialidade</th>
                <th>Telefone</th>
                <th>Registro</th>
                <th style="text-align: center;">Ações</th>
            </tr>
        </thead>
        <tbody>
            <% if (profissionais && profissionais.length > 0) { %>
                <% profissionais.forEach(profissional => { %>
                    <tr>
                        <td>
                            <img src="/uploads/profissionais/<%= profissional.foto || 'default-avatar.png' %>" 
                                 style="width: 40px; height: 40px; border-radius: 50%; object-fit: cover; border: 2px solid #e2e8f0;">
                        </td>
                        <td class="profissional-nome"><strong><%= profissional.nome %></strong></td>
                        <td><%= profissional.especialidade %></td>
                        <td><%= profissional.telefone || 'Não informado' %></td>
                        <td><%= profissional.registro %></td>
                        
                        <td style="text-align: center; white-space: nowrap;">
                            <a href="/profissionais/editar/<%= profissional.id %>" class="btn-edit" title="Ver Ficha / Editar" style="margin-right: 10px; text-decoration: none; color: #0284c7;">
                                <i class="fas fa-edit"></i>
                            </a>

                            <button type="button" class="btn-delete-compact" title="Excluir" 
                                    onclick="abrirModalExcluir('<%= profissional.id %>', '<%= profissional.nome %>')"
                                    style="background: none; border: none; cursor: pointer; color: #ef4444;">
                                <i class="fas fa-trash-alt"></i>
                            </button>
                        </td>
                    </tr>
                <% }) %>
            <% } else { %>
                <tr>
                    <td colspan="6" style="text-align: center; padding: 40px;">Nenhum profissional encontrado.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</main>

<div id="modalCadastro" class="modal-overlay" style="display: none;">
    <div class="modal-content" style="max-width: 600px;">
        <h2 style="margin-bottom: 20px;"><i class="fas fa-user-plus"></i> Novo Cadastro Profissional</h2>
        
        <form action="/profissionais/novo" method="POST" enctype="multipart/form-data">
            
            <div style="display: flex; gap: 20px; margin-bottom: 15px;">
                <div style="flex: 1;">
                    <label style="display: block; margin-bottom: 5px;">Foto</label>
                    <input type="file" name="foto" accept="image/*" style="width: 100%; padding: 7px; border: 1px solid #ddd; border-radius: 4px;">
                </div>
                <div style="flex: 2;">
                    <label style="display: block; margin-bottom: 5px;">Nome Completo</label>
                    <input type="text" name="nome" required style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                </div>
            </div>

            <div style="display: flex; gap: 15px; margin-bottom: 15px;">
                <div style="flex: 1;">
                    <label style="display: block; margin-bottom: 5px;">Especialidade</label>
                    <input type="text" name="especialidade" required placeholder="Ex: Dentista" style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                </div>
                <div style="flex: 1;">
                    <label style="display: block; margin-bottom: 5px;">Telefone / WhatsApp</label>
                    <input type="text" name="telefone" placeholder="(00) 00000-0000" style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                </div>
            </div>

            <div style="display: flex; gap: 15px; margin-bottom: 15px;">
                <div style="flex: 1;">
                    <label style="display: block; margin-bottom: 5px;">Registro (CRM/CRO)</label>
                    <input type="text" name="registro" style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                </div>
                <div style="flex: 1;">
                    </div>
            </div>

            <div style="margin-bottom: 15px;">
                <label style="display: block; margin-bottom: 5px;">Observações Internas</label>
                <textarea name="historico" rows="3" placeholder="Informações adicionais sobre o profissional..." 
                          style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px; font-family: inherit;"></textarea>
            </div>

            <div style="display: flex; gap: 10px; margin-top: 20px;">
                <button type="submit" style="flex: 1; background: #1e293b; color: white; padding: 12px; border: none; border-radius: 4px; cursor: pointer; font-weight: bold;">Salvar Cadastro</button>
                <button type="button" onclick="fecharModalCadastro()" style="flex: 1; background: #e2e8f0; padding: 12px; border: none; border-radius: 4px; cursor: pointer;">Cancelar</button>
            </div>
        </form>
    </div>
</div>

<div id="modalExcluir" class="modal-overlay" style="display: none;">
    <div class="modal-content" style="max-width: 400px; text-align: center;">
        <div style="color: #ef4444; font-size: 3rem; margin-bottom: 15px;">
            <i class="fas fa-exclamation-circle"></i>
        </div>
        <h2>Confirmar Exclusão</h2>
        <p id="textoConfirmacao" style="margin: 15px 0; color: #64748b;"></p>
        <form id="formExcluir" method="POST">
            <div style="display: flex; gap: 10px; margin-top: 25px;">
                <button type="submit" style="flex: 1; background: #ef4444; color: white; padding: 12px; border: none; border-radius: 6px; cursor: pointer; font-weight: bold;">Sim, Excluir</button>
                <button type="button" onclick="fecharModalExcluir()" style="flex: 1; background: #e2e8f0; padding: 12px; border: none; border-radius: 6px; cursor: pointer;">Cancelar</button>
            </div>
        </form>
    </div>
</div>

<script>
    function filtrarProfissionais() {
        const input = document.getElementById('inputPesquisa').value.toLowerCase();
        const rows = document.querySelectorAll('#tabelaProfissionais tbody tr');
        rows.forEach(row => {
            const nomeCell = row.querySelector('.profissional-nome');
            if (nomeCell) {
                const nomeText = nomeCell.textContent.toLowerCase();
                row.style.display = nomeText.includes(input) ? '' : 'none';
            }
        });
    }

    function abrirModalCadastro() { document.getElementById('modalCadastro').style.display = 'flex'; }
    function fecharModalCadastro() { document.getElementById('modalCadastro').style.display = 'none'; }

    function abrirModalExcluir(id, nome) {
        document.getElementById('modalExcluir').style.display = 'flex';
        document.getElementById('textoConfirmacao').innerHTML = `Tem certeza que deseja excluir o profissional <strong>${nome}</strong>?`;
        document.getElementById('formExcluir').action = `/profissionais/deletar/${id}`;
    }

    function fecharModalExcluir() { document.getElementById('modalExcluir').style.display = 'none'; }
</script>

<%- include('partials/footer') %>